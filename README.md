# GoldScalpers — site deploy branch

This branch is the **web root** for the GoldScalpers marketing site.
Upload your site source files (index.html, css/, js/, assets/, .htaccess, etc.)
directly to the ROOT of this branch — Hostinger's Git deploy maps this branch's
root to `public_html`.

You can delete this README once your files are here.