# Core dependencies for the Windows Trading Bot.
# Tkinter and http.server ship with Python, so only two third-party packages
# are required. Both are pure-python wheels and install on every Windows
# version that runs Python 3.8+.

ccxt>=4.0.0          # Unified API for Binance, Bybit, OKX, KuCoin, Bitget, ...
cryptography>=41.0   # Fernet encryption for API-key storage + PIN protection
pystray>=0.19        # Optional: minimize-to-system-tray for 24/7 running
Pillow>=9.0          # Optional: tray icon image (required by pystray)
